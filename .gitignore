<!DOCTYPE html>
<html>
<head>
    <title>Happy Birthday Puji!</title>
    <style>
        body {
            background-color: black;
            color: white;
            text-align: center;
            font-family: Arial, sans-serif;
        }
        #balloon {
            width: 50px;
            height: 70px;
            background-color: red;
            border-radius: 50%;
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            animation: moveUp 5s linear infinite;
        }
        @keyframes moveUp {
            0% { bottom: 0; }
            100% { bottom: 100%; }
        }
    </style>
</head>
<body>
    <h1>Happy Birthday!</h1>
    <p>Wishing you all the best!</p>
    <div id="balloon"></div>

    <script>
        // Balloon animation JavaScript code if needed
    </script>
</body>
</html>
